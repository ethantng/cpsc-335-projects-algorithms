README for Algorithm 2: Connecting Pairs of Persons

The user must open a terminal or command prompt.
Locate the directory with the .cpp file.
Compile the program.
Run the executable.
The program processes inputs that have already been defined in the code.
Once the program is executed, the output will show the minimum number of swaps required to correctly seat all the couples.